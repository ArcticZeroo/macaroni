enum OperatorType {
    add,
    sub,
    mul,
    mod,
    div, integerDiv,
    lessThan, lessEqual,
    greaterThan, greaterEqual,
    equal, notEqual,
    strictEqual, strictNotEqual,
    getItem, setItem
}

export default OperatorType;