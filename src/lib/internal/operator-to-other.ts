import operators from '../symbols/operators';

const operatorDisplays: {} = {
    [operators.add]: '+',
    [operators.multiply]: '*',
    [operators.subtract]: '-',
    [operators.divide]: '/'
};

export { operatorDisplays };